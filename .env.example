# App server
PORT=3000

# Admin API secret (change in production)
ADMIN_SECRET=change-me

# Recording output directory
RECORD_DIR=./recordings

# Optional: public IP for SFU deployments behind NAT
# PUBLIC_IP=
